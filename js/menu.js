let menu = document.querySelector(".nav__menu");
let links = document.querySelector(".nav__links");

console.log(menu);
console.log(links);

menu.addEvent;

menu.addEventListener("click", () => {
  links.classList.toggle("nav__link--show");
});
